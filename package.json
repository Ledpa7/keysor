{
  "name": "keysor-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm install --prefix homepage && npm run build --prefix homepage && powershell -Command \"Copy-Item -Path 'homepage/dist/*' -Destination 'public' -Recurse -Force\""
  }
}
